version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd website
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: website/dist
    files:
      - "**/*"
  cache:
    paths:
      - website/node_modules/**/*
