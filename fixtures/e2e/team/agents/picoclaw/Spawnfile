spawnfile_version: "0.1"
kind: agent
name: picoclaw

runtime: picoclaw

execution:
  model:
    auth:
      method: api_key
    primary:
      provider: openai
      name: gpt-5
  sandbox:
    mode: workspace

workspace:
  docs:
    system: AGENTS.md
